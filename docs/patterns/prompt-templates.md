# Prompt Templates Pattern

## Problem

Free-form prompts drift and produce inconsistent outputs.

## Pattern

Use parameterized templates with explicit sections: role, task, constraints, expected format, and examples.

## Example

A grounded QA template requiring source-backed answers and an uncertainty note.

## When to Use / Avoid

Use for repeatable workflows. Avoid over-constraining pure ideation tasks.
