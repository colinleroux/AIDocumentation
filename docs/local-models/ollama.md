# Ollama

## Setup

Document install steps, model pulls, and runtime config so the environment is repeatable.

## Model Comparisons

Compare models using the same prompt sets and constraints to avoid biased conclusions.

## Performance Notes

Record context length, quantization, and hardware with each test so results are interpretable.
